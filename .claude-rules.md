# Matt Pocock AI 工程化工作流规范

你必须严格遵守以下技能定义和工作流边界：

## 1. 核心技能指令定义

- **to-prd**: 将当前讨论合成一份 PRD 并提交为 GitHub Issue。**禁止在此阶段编写任何业务代码或初始化项目结构。**
- **to-issues**: 将 PRD 拆解为独立的、可领取的 GitHub Issues（采用垂直切片模式）。输出采用高密度 Markdown 汇总表（单表覆盖所有 Issue），合并草案与测验为一步，预览阶段禁止输出完整 Issue Body。
- **setup-matt-pocock-skills**: 初始化项目配置（Issue 模板、标签、CONTEXT.md 结构）。
- **grill-with-docs**: 针对当前的计划/ADR 进行压力测试，挑战领域模型和术语。
- **tdd**: 遵循 红->绿->重构 循环。必须先写测试，再写实现。
- **diagnose**: 故障诊断循环（重现 -> 最小化 -> 假设 -> 埋点 -> 修复 -> 回归）。
- **zoom-out**: 提供宏观视角，解释当前代码在整体架构中的位置。
- **improve-codebase-architecture**: 根据 CONTEXT.md 和 ADR 寻找重构机会。
- **prototype**: 构建抛弃型原型验证设计，不进入生产代码。
- **audit-consistency**: 蓝图层逻辑审计员。严格审计 PRD、CONTEXT、ADR 间的逻辑一致性，识别断头路与决策冲突。
- **reconcile-consistency**: 蓝图层决策调和专家。接收审计报告后，以最小变动原则一次性修复文档矛盾。
- **audit-tasks**: 全栈任务质量与复用审计员。在 to-issues 产出任务清单后，对任务进行 DRY、闭环、原子、顺序、遗漏五维工程合规性审计，输出高密度 Markdown 审计表格，完成后询问是否需要执行 reconcile-tasks 自动修复。
- **reconcile-tasks**: 全栈任务调和专家。接收审计报告，优化 Issue 颗粒度、逻辑依赖及复用性，输出原子化执行蓝图。内置断路器机制：最多 2 次自我审计迭代，无法收敛时触发诚实申诉（输出《决策冲突报告》），以"减少总 Critical 数"为收敛目标，允许保留 Warning/Info 换取核心路径闭环。

## 2. to-issues 输出禁令

执行 `/to-issues` 时必须遵守以下禁令：

1. **禁止输出思考过程**: "Gather context" 和 "Exploring codebase" 为后台静默步骤，禁止在终端输出进度描述。
2. **禁止分步交互**: 不再分别输出"草案"和"测验"。直接输出最终的汇总表，并询问"以上为任务提案，是否批准发布？"。
3. **禁止主观评论**: 严禁输出"我正在分析..."、"我认为这个颗粒度很合适..."等主观评论。
4. **禁止重复背景**: 严禁重复 PRD 中的背景描述。
5. **禁止预览阶段输出完整模板**: 终端预览仅展示 Markdown 汇总表（ID / 任务标题 / 3D / 原子实现路径 (Atomic Path) / 验收标准 (DoD)）。完整的 Issue Body 模板仅在发布到 Tracker 时使用。
6. **输出格式强制**: 必须使用单表 Markdown 汇总格式。3D 列使用缩写 `I/L/S`。原子实现路径按 (D)atabase → (B)ackend → (F)rontend 顺序，层间用 `<br>` 分隔。禁止在单元格中使用长句子。
7. **禁止分卡输出**: 禁止输出单独的 Issue 卡片，所有 Issue 必须汇总在一张表中。
8. **原子级颗粒度强制 (Atomic Granularity Mandate)**:
   - **(D) Storage**: 禁止 "建表"、"加字段" → 必须写成具体 `[表名]` 或 `[表名.字段名]`
   - **(B) Backend**: 禁止 "写接口"、"实现 API" → 必须写成 `[METHOD /path] -> [Service.method]`
   - **(F) Frontend**: 禁止 "写页面"、"做组件" → 必须写成 `<Component /> -> useHook` 或 `[Store名]`
9. **多实体强制列出**: 如果一个 Issue 涉及多个组件/路由/表，必须在路径中全部列出，用 `+` 连接。颗粒度太粗无法容纳的，拆分为多个子 Issue。
10. **地基阶段强制**: 必须先输出 #0 Foundation Issue（如 #0.1 共享 Service、#0.2 共享组件、#0.3 数据模型基础），识别所有可复用基础设施。禁止跳过 #0 阶段直接拆解功能 Issue。
11. **复用标注强制**: 原子路径中每个实体必须使用 `Create`（新建）/ `Extend`（扩展）/ `Consume`（复用）前缀。`Create` 仅出现在 #0 Foundation 或首次创建实体的 Issue 中；后续 Issue 引用已建实体必须使用 `Extend` 或 `Consume`。禁止在不检查 #0 和先前 Issue 的情况下盲目 Create。
12. **HITL/AFK 标题标注**: 汇总表中每个 Issue 标题必须带 `[AFK]` 或 `[HITL]` 标记。
13. **表格渲染强制 (Visual Table Enforcement)**:
    - **禁止代码块包裹**: 表格严禁使用三反引号（```）或任何代码块语法包裹。必须以原始 Markdown 直接输出。
    - **禁止转义字符**: 表格中严禁出现多余的转义字符。
    - **强制空行触发**: 表格开始前和结束后各强制一个完整空行。这是 Markdown 渲染器（如 VS Code）识别表格语法的必要条件。
    - **语法严格校验**: 每一行必须以 `|` 开头并以 `|` 结尾。对齐行（第二行）必须使用标准格式 `|:---|:---|...|`。
    - **单元格垂直换行**: 在"原子实现路径"和"验收标准"列中，必须使用 HTML 的 `<br>` 标签进行强制换行，严禁让单行内容过长导致表格横向无限延伸。
14. **中文注解强制 (Mandatory Chinese Annotation)**:
    - 原子实现路径列中每个技术标识符（表名、字段、路由、Service 函数、组件、Hook、Store）后必须紧跟 `(中文说明)`。
    - 中文说明应简洁，≤6 字，描述其业务功能。
    - 格式：`(D): Create [表.字段] (含义) <br> (B): Create [路由] (作用) -> [Service.函数] (逻辑) <br> (F): Create <组件 /> (功能) -> useHook (作用)`。
    - 路径中 `->` 表示开发先后顺序。

## 3. 强制性约束

1. **文档驱动**: 所有的架构决策必须记录在 `docs/adr/*.md`，项目全局状态记录在 `CONTEXT.md`。
2. **阶段隔离**:
   - 运行 `/to-prd` 后必须停下来，等待用户确认。
   - 只有在明确处理某个具体 Issue 时才能开始编写代码。
3. **垂直切片**: 实现功能时必须包含 DB + API + UI 的完整链路，而不是横向的分层实现。

## 4. ⚠️ 架构治理边界原则

- **蓝图治理阶段** (`audit-consistency`, `reconcile-consistency`)：只允许读写文本/文档文件，用于打磨逻辑。此阶段不应存在代码文件改动。
- **实施对齐阶段** (`tdd`, `code-alignment-audit`)：负责将代码向蓝图对齐。如果代码实现发现蓝图有误，应返回蓝图阶段进行 Reconciliation。

## 5. 响应格式

在执行任何指令前，先声明你正在使用哪个技能。例如：“正在执行 [to-prd]：正在合成 PRD 文档...”
