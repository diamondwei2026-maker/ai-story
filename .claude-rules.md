# Matt Pocock AI 工程化工作流规范

你必须严格遵守以下技能定义和工作流边界：

## 1. 核心技能指令定义

- **to-prd**: 将当前讨论合成一份 PRD 并提交为 GitHub Issue。**禁止在此阶段编写任何业务代码或初始化项目结构。**
- **to-issues**: 将 PRD 拆解为独立的、可领取的 GitHub Issues（采用垂直切片模式）。
- **setup-matt-pocock-skills**: 初始化项目配置（Issue 模板、标签、CONTEXT.md 结构）。
- **grill-with-docs**: 针对当前的计划/ADR 进行压力测试，挑战领域模型和术语。
- **tdd**: 遵循 红->绿->重构 循环。必须先写测试，再写实现。
- **diagnose**: 故障诊断循环（重现 -> 最小化 -> 假设 -> 埋点 -> 修复 -> 回归）。
- **zoom-out**: 提供宏观视角，解释当前代码在整体架构中的位置。
- **improve-codebase-architecture**: 根据 CONTEXT.md 和 ADR 寻找重构机会。
- **prototype**: 构建抛弃型原型验证设计，不进入生产代码。
- **audit-consistency**: 蓝图层逻辑审计员。严格审计 PRD、CONTEXT、ADR 间的逻辑一致性，识别断头路与决策冲突。
- **reconcile-consistency**: 蓝图层决策调和专家。接收审计报告后，以最小变动原则一次性修复文档矛盾。

## 2. 强制性约束

1. **文档驱动**: 所有的架构决策必须记录在 `docs/adr/*.md`，项目全局状态记录在 `CONTEXT.md`。
2. **阶段隔离**:
   - 运行 `/to-prd` 后必须停下来，等待用户确认。
   - 只有在明确处理某个具体 Issue 时才能开始编写代码。
3. **垂直切片**: 实现功能时必须包含 DB + API + UI 的完整链路，而不是横向的分层实现。

## 3. ⚠️ 架构治理边界原则

- **蓝图治理阶段** (`audit-consistency`, `reconcile-consistency`)：只允许读写文本/文档文件，用于打磨逻辑。此阶段不应存在代码文件改动。
- **实施对齐阶段** (`tdd`, `code-alignment-audit`)：负责将代码向蓝图对齐。如果代码实现发现蓝图有误，应返回蓝图阶段进行 Reconciliation。

## 4. 响应格式

在执行任何指令前，先声明你正在使用哪个技能。例如：“正在执行 [to-prd]：正在合成 PRD 文档...”
