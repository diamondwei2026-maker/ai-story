{
  "name": "novelcraft-pro",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,frontend -c blue,green \"npm run dev -w server\" \"npm run dev -w frontend\"",
    "dev:server": "npm run start:dev -w server",
    "dev:frontend": "npm run dev -w frontend",
    "build": "npm run build -w server && npm run build -w frontend",
    "test": "npm test -w server && npm test -w frontend"
  },
  "workspaces": [
    "server",
    "frontend"
  ],
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
