# Vite 生产环境变量 — 构建时注入
#
# 当前部署方式: 前端静态文件与后端由同一台服务器 nginx 托管,
# 前端默认请求同源 /api, nginx 反代到后端, 无需跨域。
# 因此这里留空, 前端代码会回退到默认值 '/api' (见 frontend/src/api/common.ts)。
# 若以后前端单独部署(如 Vercel), 再在此处或对应平台仪表板设置完整后端地址。
VITE_API_BASE_URL=
