# 蓝图逻辑审计报告 — 前端 UI 中文化一致性检查

审计日期：2026-05-29
审计范围：`d98edf1` 提交（前端 UI 文案全量中文化）与项目文档一致性

### Blocker（必须先解决）

无。

### Warning（存在隐患）

- **W1 术语表 "强制标记 DISPUTED" 与 UI 文案不同步**：前端 UI 已将 `DISPUTED` 统一替换为 `争议`（DisputeConfirm.vue、ReviewPanel.vue），但根 CONTEXT.md 中仍有两处沿用 `DISPUTED`——行 30 `强制标记 DISPUTED`、行 60 `强制 DISPUTED`。用户阅读术语表时看到的仍是英文码，而 UI 已展示中文。涉及：`CONTEXT.md`
- **W2 src/step/CONTEXT.md 内部 DISPUTED 表述不统一**：行 26 已用 `用户强制标记争议`（中文），但行 37 `可强制 DISPUTED`、行 71 `强制标记 DISPUTED` 仍用英文码。同一文档内部术语风格不一致。涉及：`src/step/CONTEXT.md`

### Info（优化建议）

- **I1 PRD 中 DISPUTED 沿用技术码属合理范围**：PRD (`docs/prd/novelcraft-pro.md`) 在多处使用 `强制标记 DISPUTED`（行 34、79、80、285、286、288）。由于 PRD 是技术规格文档，使用内部状态码表述属惯例，与 UI 中文化不构成实质冲突。建议 PRD 仅在下次修订时顺带统一。
- **I2 ChapterStatus 枚举值 `DISPUTED` 仍为英文**：Chapter 状态枚举值 `DISPUTED` 作为后端 API 字段值保留英文（与 `PENDING`/`DRAFT`/`REVIEWING`/`COMPLETED` 一致）。前端使用 `statusLabel` 函数等映射为中文展示，这在 `ProjectHubView.vue` 的 `statusLabel` 函数（ARCHIVED→已归档 等）已有先例，设计一致。

---

**审计统计**：B: 0 | W: 2 | I: 2
**结论**：逻辑已闭环，无需 reconcile。W1/W2 为文档术语同步问题，不阻塞代码。建议在 CONTEXT.md 中将用户面描述统一为"争议"，保留技术码 `DISPUTED` 在 API/状态枚举上下文中使用。
