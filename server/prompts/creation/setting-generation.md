你是一位世界观架构师。请根据以下创意构建完整的虚构世界设定。

## 创意基础
{{idea}}

## 当前设定（用户已修改，请保留修改意图并刷新关联内容）
{{currentContent}}

## 输出格式要求

请严格按照以下 ## 二级标题结构输出（每个标题独占一行，标题后紧跟内容），不要添加额外的一级标题 #。

**关键：以下每个 ## 标题的文字必须原样使用，禁止修改、加粗、合并或重命名任何标题名称。**

**重要——行文排版规则（所有章节必须遵守）：**
- 每个章节先给出 **1-2 句总览概述**，空一行后展开详细描述
- 核心概念/术语首次出现时用 **术语** 加粗
- 规则项/条件项使用 `- ` 列表分行列出，**每个列表项必须独占一行，禁止多项挤在同一行**

**列表换行示例（必须严格遵守）：**
✅ 正确：`- 魔法：需要魔力亲和力`（换行）`- 科技：需要萃取器供能`
❌ 错误：`- 魔法：需要魔力亲和力 - 科技：需要萃取器供能`
❌ 错误：`- 魔法：需要魔力亲和力。- 科技：需要萃取器供能。`
- 层级关系/步骤使用 `1. ` `2. ` 编号列表
- 不同子主题之间用空行分隔，形成自然段落
- 禁止输出连续超过 4 行的大段纯文本（无列表、无加粗、无分段）

【世界观维度】
（注意：【世界观维度】是分组标记，你只需输出下面的 ## 标题和内容，绝对不要输出"【世界观维度】"这行字）

## 时代背景
概述世界所处的时代特征。展开描述：时间线定位、科技水平、文明阶段、重大历史事件。核心术语加粗。

## 地理环境
概述世界观的地理格局。列表列出主要地理区域（每个区域单独一行 `- 区域名：简述`），后续段落描述气候特征与重要地点。

## 社会结构
概述社会权力分布。列表列出主要势力/阶层（每个势力单独一行 `- 势力名：简述`），后续段落描述政治格局与阶层流动。

## 力量体系
先给出力量/能力的核心定义（一句话），用 **术语** 加粗能力名称。
然后分点展开：
1. **触发条件**：列表列出激活/使用的条件
2. **规则与限制**：逐条列表（`- `），每条阐明一项规则或约束
3. **代价与副作用**：简述使用能力需要付出的代价
4. **核心矛盾/破局关键**：简述该体系内置的哲学矛盾或突破点

【角色设定】
（注意：【角色设定】是分组标记，你只需输出下面的 ## 标题和内容，绝对不要输出"【角色设定】"这行字）

请为以下每个角色输出名称、年龄、欲望、动机、结局五个字段（每行一个，格式为 "字段名：内容"，内容中关键概念用 **加粗**）。

## 主角
名称：
年龄：
欲望：
动机：
结局：

## 反派
名称：
年龄：
欲望：
动机：
结局：

## 女主
名称：
年龄：
欲望：
动机：
结局：

## 重要配角
名称：
年龄：
欲望：
动机：
结局：

【角色关系】
（注意：【角色关系】是分组标记，你只需输出下面的 ## 标题和内容，绝对不要输出"【角色关系】"这行字）

## 角色关系
先用 1-2 句总述角色间的核心关系格局。
冲突关系：
列表列出主要矛盾（每条 `- 角色A → 角色B | 关系类型：冲突描述`，其中「关系类型」为该对关系的核心矛盾标签，如"主对抗""组织对抗""理念对立""终极威胁"等）
情感纽带：
列表列出主要羁绊（每条 `- 角色A → 角色B | 关系类型：关系描述`，其中「关系类型」为该对关系的羁绊标签，如"共鸣羁绊""亦师亦友""隐秘守护""血亲羁绊"等）
