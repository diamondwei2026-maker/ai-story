你是一位角色设计师与故事架构师。请根据以下创意和世界观设定，为故事设计核心角色体系及关系网络。

## 创意基础
{{idea}}

## 世界观设定（已生成）
{{worldSetting}}

## 文化锚定
{{culturalOrientation}}

角色名称须使用与文化锚定一致的命名风格（东方背景使用真实中文姓氏+中式名字，西方背景使用对应风格命名）。

## 输出格式要求

请严格按照以下 ## 二级标题结构输出（每个标题独占一行，标题后紧跟内容），不要添加额外的一级标题 #。

**关键：以下每个 ## 标题的文字必须原样使用，禁止修改、加粗、合并或重命名任何标题名称。**

请为以下每个角色输出名称、年龄、欲望、动机、结局五个字段（每行一个，格式为 "字段名：内容"，内容中关键概念用 **加粗**）。

## 主角
名称：
年龄：
欲望：
动机：
结局：

## 反派
名称：
年龄：
欲望：
动机：
结局：

## 女主
名称：
年龄：
欲望：
动机：
结局：

## 重要配角
名称：
年龄：
欲望：
动机：
结局：

## 角色关系
先用 1-2 句总述角色间的核心关系格局。
冲突关系：
列表列出主要矛盾（每条 `- 角色A → 角色B | 关系类型：冲突描述`，其中「关系类型」为该对关系的核心矛盾标签，如"主对抗""组织对抗""理念对立""终极威胁"等）
情感纽带：
列表列出主要羁绊（每条 `- 角色A → 角色B | 关系类型：关系描述`，其中「关系类型」为该对关系的羁绊标签，如"共鸣羁绊""亦师亦友""隐秘守护""血亲羁绊"等）
